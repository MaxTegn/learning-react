import React from 'react'

function Conditional(props) {
    // condition ? statement if if true : statement if false
    return <h1>Some cool stuff about conditional</h1>
}

export default Conditional